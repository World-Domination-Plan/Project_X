using UnityEngine;
/*
public class GalleryTester : MonoBehaviour
{
    // Drag your Canvas Prefab (from the scene) here
    public ArtworkCanvas canvasToTest; 

    // Drag one of your ScriptableObject Data files here
    public ArtworkMetaData2D testData;

    void Start()
    {
        if (canvasToTest != null && testData != null)
        {
            // This calls your backend function to load the art
            canvasToTest.LoadArtwork(testData);
            Debug.Log("Test: Artwork loaded successfully!");
        }
        else
        {
            Debug.LogError("Test: Please assign the Canvas and Data in the Inspector!");
        }
    }
}
*/